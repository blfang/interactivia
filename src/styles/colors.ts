// JS-consumable color constants for contexts that can't reference CSS custom
// properties (Plotly chart traces, inline SVG attributes). Mirrors the
// corresponding custom property in src/styles/theme.css where one exists.
export const COLOR_PRIMARY = '#3b82f6';    // theme.css --color-primary
export const COLOR_SUCCESS = '#10b981';    // theme.css --color-success
export const COLOR_DANGER = '#dc2626';     // theme.css --color-danger
export const COLOR_TEXT_MUTED = '#64748b'; // theme.css --color-text-muted
